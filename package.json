{
  "name": "skill-trading",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p backend/public && cp -r frontend/dist/* backend/public/ && cd backend && npm install && npx prisma generate && npx prisma migrate deploy",
    "start": "cd backend && node src/server.js"
  },


  "dependencies": {
    "vite": "7.3.2",
    "@vitejs/plugin-react": "5.1.1",
    "@tailwindcss/vite": "4.1.17",
    "tailwindcss": "4.1.17"
  }
}